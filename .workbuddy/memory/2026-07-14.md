# 2026-07-14 工作日志

## 技能安装记录
本次为 BMI 项目安装/启用了以下技能（Skills）：

- **course-manager** — 课程管理器，统一管理与维护课程内容数据（大纲、章节、知识点、知识图谱、参考课件），支持 SQLite 持久化与 CRUD，可导出标准课程大纲 HTML 及 course-page-generator 兼容 JSON。
- **agent-team-orchestration** — 多智能体团队编排，定义角色、任务生命周期、交接协议与评审流程；用于搭建 2 个及以上协作 agent 的团队工作流。
- **github** — 通过 `gh` CLI 与 GitHub 交互（issue / PR / run / api），处理仓库、拉取请求、CI 及高级查询。

## 项目初始化（/init-project）
- 分析仓库：Java MVC 脚手架「BMI 体质评估与预测系统」，多数目录仅 `.keep` 占位。
- origin: https://github.com/Saints2026/BMI.git
- 结构：controller / model(ai,db) / view / test，依赖走 `lib/`（无 Maven/Gradle）。
- 敏感文件（gitignore）：db-config.properties、ai-key.properties、*.log。
- 已将项目概况、结构与约定固化到 `.workbuddy/memory/MEMORY.md`。

## course-manager 初始化
- 初始化本地课程数据库：`C:\Users\ASUS\.workbuddy\skills\course-manager\data\courses.db`。
- 本地库已存在课程：ID8「01 Agentic数字团队-产品经理」、ID7「05 Vibe Coding技能使用」、ID6「04 Vibe Coding进阶对话技巧」、ID5「03 Vibe Coding项目宪章」、ID4「02 Vibe Coding核心工作流」、ID3「01 Vibe Coding开发技术基础」、ID2「Agentic数字团队」。
- 检查 BMI 项目内课程素材：docs/、lib/ 仅 `.keep` 占位，项目内**无**课程素材文件（仅 README/.gitignore）。
- 结论：课程知识库来源为 course-manager 本地库，非项目内文件。
- **已绑定**：将本地库课程 ID 2~8（01~05 系列 + Agentic数字团队总纲）映射写入 `MEMORY.md`，作为 BMI 工作空间的课程知识库引用。

## SDD 全套规范文档（多Agent协同）
- 用 agent-team-orchestration 编排：Orchestrator(本体) + 产品经理Agent + 架构师Agent。
- 新建 **CODEBUDDY.md（项目宪章）**：Java8+ / 自研MVC / Swing / JDBC+SQLite / HttpURLConnection AI / JUnit，jar入lib（无Maven）；命名规则 com.bmi.<layer> + Controller/View/Dao/Client 后缀；表名 t_user/t_record；密钥读 properties 不硬编码；禁止 Web/Spring/ORM 等白名单外技术。
- **docs/spec.md**（产品Agent）：US-01~08、FR-01~07（登录/录入/BMI/体脂Deurenberg/历史/折线图/AI建议）、AC-01~07 量化验收。
- **docs/plan.md**（架构Agent）：MVC三层时序、16类接口清单、t_user/t_record DDL+索引、AI请求/响应JSON+降级、命名规则、选型(Swing自绘+SQLite)、FR全覆盖追溯。
- **docs/tasks.md**（架构Agent）：UI/数据/AI 三模块，开发A/B/C 分工，07-14~07-24 逐日计划(M1~M7里程碑)，看板 T-01~T-16 关联 FR/AC。
- 三份文档层层追溯一致，均严格遵循 CODEBUDDY.md。

## AI 健康建议接口详细设计（架构Agent）
- 调度架构 Agent 依据 docs/plan.md 产出 **docs/ai_design.md** v1.0。
- ①请求入参 `AiRequest`：`systemPrompt` + `userMetrics`(8字段) + `historyTrend` + `modelParams`，含完整 JSON 示例；②返回格式 `AiHealthResult`(9字段含饮食/运动/健康三段)；③四类异常处理：断网(ConnectException 不重试) / 超时(SocketTimeout 重试1次) / 参数为空(不重试) / 服务器5xx(重试1次) + 密钥缺失抛 AiConfigException，统一错误码表+降级文案（真诚、不暴露技术栈）；④HttpURLConnection 双超时 10s 对齐 AC-07；⑤`AiHealthClient.send()` 主流程骨架含 try-catch 分支。
- 全程对齐 plan.md §5/§6 与 spec AC-07，未引入白名单外技术。

## 数据库详细设计（DBA Agent）
- 调度 DBA Agent 依据 docs/plan.md 产出 **docs/db_design.md** v1.0。
- 两表：`user`(7字段: id/username/password_hash/salt/created_at/updated_at/status) 与 `body_record`(8字段: id/user_id/measure_time/height/weight/bmi/body_fat/created_at)；E-R 关系 user(1)─<body_record(0..N)，外键 user_id→user.id ON DELETE CASCADE。
- 给出 SQLite + MySQL 双方言可执行建表 SQL（含 PRAGMA/InnoDB、CHECK 区间约束、唯一索引 username、联合索引 (user_id,measure_time)、字段 COMMENT）；含 E-R 图、字段注释汇总、FR/AC 追溯、敏感字段只存哈希。
- **命名偏离**：用户指定表名 `user`/`body_record`，偏离宪章 `t_` 前缀约定（`t_user`/`t_record`），文档已专节说明并给两种统一建议；`user` 为 MySQL 保留字，已用反引号/双引号包裹处理。

## 宪章 v1.0 统一修订（对齐 plan.md / ai_design.md / db_design.md）
- 重写 **CODEBUDDY.md v1.0**：统一以下偏离点并同步更新全部下游文档：
  - **UI 技术栈**：Swing → **JavaFX**（含 LineChart 折线图），视图类 LoginView/InputView/ChartView/MainView
  - **表名**：`t_user`/`t_record` → **`user`**/**`body_record`**（无前缀），MySQL 下 user 用反引号
  - **实体**：`Record` → **`BodyRecord`**
  - **AI 封装类名**：`AiHealthClient` → **`AiService`**
  - **视图类名**：MainFrame/RecordInputPanel/HistoryPanel/ChartPanel → MainView/InputView/**ChartView**
  - **图表方案**：Swing 自绘/JFreeChart → **JavaFX LineChart**
- 同步修订了 **plan.md** 全部引用（MVC 图、接口清单、DDL、命名表、FR 追溯、选型说明）与 **ai_design.md**（类图/方法签名/AiHealthClient→AiService）及 **db_design.md**（上游引用 t_user→user）。

## UI 设计文档（ui_design.md）
- 依据 CODEBUDDY.md / plan.md / db_design.md / ai_design.md + 用户明确的双语界面需求，产出 **docs/ui_design.md** v1.0。
- 结构：一、全局规范（双语 I18n 切换与文字绑定、通用控件样式、数值校验、全局异常弹窗、本地图片仅存路径不预览不入库）；二、8 个分页面详细设计（登录注册含本地4位验证码/注册自动算BMI/重复拦截、首页侧边导航、录入4折叠面板+实时BMI+改历史、历史统计3折线图+实时刷新、AI分析四类异常提示+本地缓存、照片管理、报告导出HTML、系统设置）；三、图表专项（ChartView LineChart、同步更新、自适应无数据提示）；四、Controller 对接约束（对齐 AiService/BodyRecord 参数、字段统一映射）。
- **扩展字段标记**：UI 引入的身体围度/健康指标(血压心率)/内脏脂肪/既往疾病/照片路径 **超出当前 body_record 表结构**，已在文档四.2 明确标记「需修订 db_design.md 扩展列或独立表」，并列出所需新增控制器(RecordController.updateRecord/PhotoController/ReportController/SettingController)。

## db_design.md v1.1 修订（用户指令：落实 ui_design.md 全部扩展字段）
- 重写 **docs/db_design.md**（v1.1），完整覆盖 6 项用户要求：
  1. **body_record 扩 10 字段**：waist_circum/hip_circum/neck_circum/wrist_circum（围度，NUMERIC 选填）、systolic_bp/diastolic_bp/heart_rate（体征，INTEGER 选填）、visceral_fat（内脏脂肪等级 1-59）、diseases（既往疾病逗号分隔 TEXT）、photo_path（照片本地路径 TEXT）；均可空、带 CHECK 区间约束；SQLite+MySQL 双方言 DDL 已更新。
  2. **分页索引**：新增 `idx_record_user_id(user_id, id DESC)` 优化历史分页；保留 `idx_record_user_time(user_id, measure_time)`；附两种分页 SQL 示例（带时间筛选/全量倒序）。
  3. **BodyRecord 实体**：新增上述 10 个扩展属性（包装类型 Double/Integer 以区分 null 与 0）+ 标准 get/set；age/gender 仍瞬时不持久化。已改 `src/com/bmi/model/BodyRecord.java`。
  4. **RecordDao/RecordController update**：RecordDao 新增 `update(BodyRecord)`（按 id+user_id 覆盖全列防越权）+ `queryByUserPage` 两分页方法；InMemoryRecordDao 同步实现；RecordController 新增 `updateRecord`/`queryRecordsPage`。已改 RecordDao/InMemoryRecordDao/RecordController.java。
  5. **新增控制器分层职责**：文档第 8.3 节给出 PhotoController（仅写 photo_path）/ ReportController（聚合 HTML 报告）/ SettingController（配色/语言/资料）的职责、依赖与核心方法签名（未生成实体代码，按用户要求为「说明」）。
  6. 统一格式、直接覆盖原文；E-R 图、字段注释汇总表、FR/AC 追溯、命名对齐、敏感字段处理全部刷新。
- **命名终决**：表名 `user`/`body_record` 已确认无 `t_` 前缀为最终决策，db_design.md §6.2 由「偏离待定」改为「已确认为最终」。
- **围度字段澄清**：用户明确指定「颈围 neck_circum / 腕围 wrist_circum」；已同步把 ui_design.md 原「臂围 arm_circum」3 处改为「腕围 wrist_circum」保持文档集一致。
- **编译验证**：`javac -encoding UTF-8` 通过（COMPILE_OK）。注意：源码含中文注释，构建须用 `-encoding UTF-8`（项目尚未建构建配置）。

## DAO 由内存实现替换为 JdbcRecordDao（MySQL JDBC 生产实现）
- **用户指令**：把 InMemoryRecordDao 替换为真实 MySQL JDBC 实现，配套连接工具类、异常捕获、资源关闭；并据 ui_design.md 生成 PhotoController/ReportController/SettingController 三类控制器代码。
- **新增 `src/com/bmi/model/db/DbUtil.java`**：读 `db-config.properties`（classpath 优先→工作目录），兼容 mysql/sqlite（`db.type`），SQLite 自动 `PRAGMA foreign_keys=ON`，提供 `closeQuietly` 资源关闭；配置/连接失败抛自定义 `DbException`（源码零硬编码）。同时新增 `DbException.java`。
- **新增 `src/com/bmi/model/db/JdbcRecordDao.java`**（实现 RecordDao）：insert(RETURN_GENERATED_KEYS 回填主键) / queryByUser(时间区间升序) / queryByUserPage(id DESC 分页) / queryByUserPage(时间区间+measure_time DESC 分页) / deleteById / update(全列 + `WHERE id AND user_id` 防越权) / findLatest / **findById(新增)**。所有 SQL 用反引号包裹（兼容 MySQL 保留字与 SQLite）。扩展字段包装类型→`ps.setNull(Types.*)`，保证 null 写 NULL（区分未录入与 0 值）。
- **RecordDao 接口**：新增 `findById(long id)`；注释改为指向 JdbcRecordDao 生产实现。
- **删除 `src/com/bmi/model/db/InMemoryRecordDao.java`**（按「替换」指令移除，已确认无外部耦合引用）。
- **生成三类控制器（均对齐 ui_design.md 六/七/八章）**：
  - `PhotoController`：bindPhoto(long recordId,long userId,String srcLocalPath) 复制图片到 `user.home/bmi/photos/{recordId}_{ts}.ext` 并仅写 `photo_path`；unbindPhoto(置 NULL+可选删本地)；getPhotoPath；绑定前 `findById` 归属校验防越权；路径白名单拒绝 http/ftp。
  - `ReportController`：exportHtml(long userId,Timestamp start,Timestamp end,ReportOptions) 聚合 RecordController 数据 + ChartController 趋势 + AiController 建议，生成单文件 HTML（内联 SVG 折线），存 `user.home/bmi/reports/bmi_report_{userId}_{date}.html`；ReportOptions 控制章节开关。
  - `SettingController`：setTheme/getTheme、setLangDefault/getLangDefault（写 `app-config.properties`）；updateProfile/getProfile（按用户隔离存 `user-profile.properties`，不改动 user 表）；不触碰密钥明文。
- **db_design.md 同步**：v1.1 变更摘要补「InMemoryRecordDao→JdbcRecordDao 替换 + 新增 findById + 配套 DbUtil/DbException」；§8.2.1 接口清单补 findById、update 实现说明改为 JdbcRecordDao。
- **新增 `db-config.properties.example`** 模板（MySQL 连接示例，不被 gitignore），供用户复制为 db-config.properties 使用；实际 JDBC 驱动 jar 需放入 `lib/`（mysql-connector-j）。
- **编译验证**：`javac -encoding UTF-8 -d /tmp/bmiclass` 编译 model+controller 全包 **COMPILE_OK**。注意：view 层尚未完成，未纳入本次编译。

## 项目快照保存（SNAP-20260714-001）
- 用户指令：将全部文档、Java 源码、已加载技能写入 BMI 项目持久记忆，生成快照标记并输出快照编号。
- 生成快照文件 `.workbuddy/memory/SNAPSHOT-20260714-001.md`：含快照标记（SNAPSHOT_ID / 时间 / 技术栈 / 文件计数 / 编译状态）、文档清单（7 篇 + SQL + 配置示例，含行数/MD5）、源码清单（31×.java + 2×.properties，按 controller/model/i18n/view 分组，含行数/MD5）、技能表、架构要点、校验与恢复指引。
- 校验值：对全部 9 个文档/SQL/配置 + 33 个源码文件计算 MD5（Git Bash `md5sum`），写入快照表；可用于后续 `md5sum -c` 完整性校验。
- **技能核对修正**：实际已安装 Skill 仅 course-manager / agent-team-orchestration / github 三项；用户所列 `jdbc`、`javafx-view-generator` 经磁盘核查**未安装**，实为项目已实现的两大代码能力模块，已在快照与 MEMORY.md 中明确标注，避免误当 Skill。
- 同步在 `MEMORY.md` 新增「项目快照索引」段落，记录本快照编号与下一编号规则。
- 临时校验清单 `_snap_manifest.tmp` 已清理。

## 快照加载与完整性校验（用户指令：加载 SNAP-20260714-001 恢复上下文）
- 读取 `.workbuddy/memory/SNAPSHOT-20260714-001.md`，重建项目完整上下文（文档/源码/技能三类资产）。
- **MD5 校验**：生成 `.workbuddy/SNAP-20260714-001.manifest` 并执行 `md5sum -c`，**42/42 文件全部 OK、0 失败**——证明磁盘文件与快照生成时刻逐字节一致，无损坏或遗漏。
- **技能核对**：course-manager / agent-team-orchestration / github 三项用户在级 Skill 目录均存在（SKILL.md 在位），与快照「已安装」一致。
- **依赖状态确认**：`lib/` 仍为空（仅 `.keep`），JavaFX SDK / mysql-connector-j / JUnit jar 尚未放入；与快照记录一致，运行/测试前需补齐。
- 结论：上下文已完整恢复并可信任；下一步编号应为 SNAP-20260714-002。

## feature-ai 校验（对照 ai_design.md + plan.md，结构化整改清单）
- **环境限制**：`gh` CLI 未安装、GitHub 连接器断开；`git branch -a` 仅 `main`/`origin/main`，无 `feature-ai` 分支。故以**工作区未提交 AI 模块源码**作为 feature-ai 内容做静态审查。
- **符合项 12 项**：命名/包、AiRequest 四段、AiHealthResult 9 字段+6 错误码、四类异常处理+重试策略、五条降级文案逐字一致、HttpURLConnection 双 10s 超时+配置外置、密钥安全(.gitignore 覆盖 ai-key.properties)、requestAdvice 不向上抛、AiController 编排、hasThreeSections 门禁 全部达标。
- **整改清单（落盘 `.workbuddy/feature-ai-review.md`）**：
  - 🔴 P0 G1：`AiService.java:62-63` 历史趋势取 `subList(0,min(size,10))` 取的是**最旧** N 条（queryByUser 升序），违背 §3.3「近 N 次」，应取末尾 N 条（最新）。
  - 🟠 P1 G2：`parseResponse` 未解析 `usage`(token 用量)，usage 恒 null（§4.1/§4.2 缺失）。
  - 🟠 P1 G3：源码缺 `BmiCalculator`/`BodyFatCalculator` 独立类（plan §4.4/§8 要求），classify 内联于 AiService（项目级架构偏差）。
  - 🟡 P2 G4：手工 JSON 解析不处理转义 `\"`；G5：readApiUrl/readModelDefault 静默回退默认值且无日志。
  - 🟢 P3 G6~G9：控制器未显式 catch AiConfigException（功能等价但文档不符）、buildRequest 的 User 参数未用、近 N 次文案、排序契约未断言。
  - 🔵 G10：view 层未核验是否接线 AiController.getAdvice（FR-07 端到端待补）。
- **结论**：核心骨架正确；仅 G1 为必须修复功能缺陷；无编译/依赖阻塞（lib/ 仍缺 JavaFX/JDBC/JUnit jar）。

## feature-ai 全量校验（MD5 复核 + 需求追溯矩阵，输出完整报告）
- **证据基础**：重算 AI 模块 6 文件 MD5，全部与 SNAP-20260714-001 逐字节一致 → 源码自快照后**未改动**，"同步后"未改变 AI 代码。
- **依赖契约核验**：`RecordDao.queryByUser` 接口注释明确「按 measure_time 升序」(RecordDao.java:21)，坐实 G1 取最旧N条之判定；`UserDao.findById` 存在；`BodyRecord` getter 齐全；全仓 grep 确认 `BmiCalculator`/`BodyFatCalculator` 类**不存在**（calcBmi/predictBodyFat 在 RecordController 静态方法、classify 内联 AiService），坐实 G3。
- **产出**：`.workbuddy/feature-ai-verification.md` —— 含 ai_design §3~§8 + plan §4.4/§6 全量需求追溯矩阵（PASS/FAIL 逐条）+ 整改清单 G1~G10 + 结论。结论：仅 G1(P0) 必须修复；G2/G3(P1) 应修复；G4/G5(P2)；G6~G9(P3)；G10 视图接线另轮核验。无编译/依赖阻塞。
- 本报告与 `feature-ai-review.md` 结论一致，为合并的全量版。

## feature-ai 分支全量静态审查（第三轮，真实分支态）
- **分支态变化**：`git branch -a` 现在同时存在本地 `feature-ai` 与 `origin/feature-ai`（之前两轮时不存在）。但**全部 AI 源码仍为 `??` 未提交**，`git diff --stat HEAD` 为空 → feature-ai 在 git 历史中不含 AI 实现，PR 实际为空（记为 P2-7）。
- **AI 包实际文件（8 java + 重复实体）**：AiService / AiHealthClient / AiRequest / AiHealthResult / AiCacheUtil / TestAiService / AiException / AiConfigException + `model/ai/BodyRecord.java`(错误副本)。比快照期多出 AiHealthClient、AiCacheUtil、TestAiService、错误 BodyRecord。
- **重大发现（此前两轮未暴露，因那时只有 6 旧文件）**：
  - 🔴 **P0-1 编译阻断**：`AiService` 引用 `AiRequest.UserMetrics/HistoryTrend/ModelParams` 内嵌类、`setUserMetrics/setHistoryTrend/setModelParams` setter、`AiHealthResult.ok()/setFinishReason()/hasThreeSections()` 与分段字段——这些在现有 `AiRequest.java`/`AiHealthResult.java`（简化版，仅 success/fail/isValid）中**均不存在** → `AiService`+`AiController`+`BmiApplication` 无法 `javac`。
  - 🔴 **P0-2 文档冲突**：`docs/ai_design.md` 含未解决合并冲突标记（`<<<<<<< HEAD`/main 详细版混排），以详细版(SDD-AI v1.0)为基线。
  - 🔴 **P0-3 架构分裂**：同时存在 `AiService`(详细签名版,但DTO未实现→坏)与 `AiHealthClient`(简化版,可编译但仅被 TestAiService 调用=死代码)；设计(plan §4.4/ai_design §7.1)指定封装类为 `AiHealthClient`。建议统一为 AiHealthClient、删 AiService、补齐 DTO。
  - 🟠 **P1**：历史趋势取最旧N条(AiService:62-63,同前G1)；AiHealthClient 请求缺 bmi/bmiGrade/bodyFat/measureTime(:199-218)；`loadConfig` 抛 RuntimeException 而非 AiConfigException(:45-65，破坏分层契约)；三段校验仅要求"任一存在"(:263)；usage 未解析；`model/ai/BodyRecord.java` 重复错误实体；缺独立 BmiCalculator/BodyFatCalculator。
  - 🟡 **P2**：AiCacheUtil 死代码；TestAiService 非 JUnit 且包名错；配置读取策略两实现不一致；默认模型 gpt-4o-mini vs deepseek-chat；escapeJson 两实现不一致；plan/ai_design 用 Swing 与宪章 JavaFX 冲突；分支未提交。
- **符合项**：四类异常+重试骨架、双10s超时、Bearer鉴权配置外置、五条降级文案逐字一致、AiException/AiConfigException 继承、AiController 编排、OpenAI 报文格式 —— 均达标。
- **产出**：`.workbuddy/feature-ai-review-20260714.md`（P0/P1/P2 结构化清单 + 修复顺序）。
- **结论**：feature-ai 当前不可编译、不可合并、不可评审；须先 P0-2 解冲突→P0-3/P0-1 统一封装类并补齐 DTO→P1 功能性修复→P2 清理→提交分支。

## 快照 SNAP-20260714-001 完整还原（用户指令：加载快照、完整恢复全部资产）
- **诊断**：`feature-ai` 分支工作区已严重偏离快照——4 篇文档被删、3 文件被改、新增污染文件（含 `ai-key.properties` 真实密钥已落入 `cfe1efb` 提交历史）。快照清单仅存 MD5、不含内容，故还原需回溯 git。
- **回溯结论**：6 篇文档 + `db_design.md` 可从 `origin/main`/悬空 blob(`9333e7d6`→`db_design.md` ac8a4923) 还原；但 `AiRequest.java`(54aacf51)、`AiHealthResult.java`(7c786074) 的快照字节在**全部 commit/index/对象库均不存在** → 从未提交且已被覆盖，**永久丢失**。
- **执行**：① 6 篇文档从 origin/main 写出（逐字节匹配）；② db_design.md 从悬空 blob 写出；③ .gitignore 从 origin/main 还原基线；④ 污染文件移至仓库外隔离区 `D:\Users\Desktop\BMI\_quarantine_feature-ai_20260714\`（AiHealthClient/AiCacheUtil/TestAiService/重复BodyRecord/lib jars/docs/ai_config.md）；⑤ `ai-key.properties` 移至隔离区并改名为 `*.QUARANTINED_SECRET_DO_NOT_COMMIT`。
- **最终校验**：缺失 0、残留污染 0、**逐字节匹配 40/42**、技能三项在位。
- **唯一缺口**：AiRequest/AiHealthResult 为 feature-ai 版本（非快照字节，且缺 AiService 所引用的 UserMetrics/HistoryTrend/ModelParams/ok()/hasThreeSections()）→ AI 模块当前无法编译。
- **安全事件**：API Key 已泄露于 feature-ai 历史，**须立即轮换**；工作区副本已隔离防复发提交。
- **产出**：`.workbuddy/SNAP-restore-20260714.md`（还原报告+隔离清单+密钥告警+后续建议）。
- **后续**：建议依 ai_design.md §3 重建两 DTO 以使 AiService 恢复编译（原字节不可得）；清理 feature-ai 污染历史；补齐 lib/ 依赖。

## 生成 db/sqlite_init.sql（SQLite 方言，对齐 MySQL 版）
- **用户指令**：从 db_design.md 提取 SQLite 方言建表语句，输出独立完整 `db/sqlite_init.sql`，字段与约束对齐 `db/mysql_init.sql`。
- **做法**：以 db_design.md §4.1 为基础，强化与 MySQL 版(§4.2)对齐——
  - user.created_at / updated_at 补 `NOT NULL`（对齐 MySQL）；
  - 全部 CHECK/UNIQUE/外键约束**显式命名**（chk_user_status / chk_record_* / idx_*），与 MySQL 版约束名一一对应；
  - 字段中文注释以 `--` 行注体现（SQLite 不支持 COMMENT，对齐 MySQL 行内 COMMENT 语义）；
  - 保留 `PRAGMA foreign_keys=ON`、AUTOINCREMENT、3 个索引（含 `idx_record_user_id(user_id,id DESC)` 分页索引）。
- **验证**：①去重字段集与 mysql_init.sql **完全一致**(24 列逐一对应)；②Python 内置 sqlite3 真实执行：两表创建、外键 `ON DELETE CASCADE` 生效、3 索引齐备、CHECK 约束(`chk_record_height`/`chk_user_status`) 正确拦截非法值。脚本可直接执行。
- 文件位置：`D:\Users\Desktop\BMI\BMI\db\sqlite_init.sql`（新增，未跟踪）。

## 生成 ui_lib_record.md（第三方依赖清单）
- **用户指令**：整理项目运行所需全部第三方 jar 清单（JavaFX SDK / mysql-connector-j / JUnit5），给出版本、下载地址、lib 存放规范，并创建/更新 `ui_lib_record.md`。
- **事实依据**：`DbUtil.getConnection()` 按 `db.type` 兼容 mysql/sqlite 两种方言（JDBC4 自动注册驱动，无 Class.forName）；`lib/` 当前仅 `.keep`；`.gitignore` 未忽略 `lib/*.jar`（jar 会入库）；feature-ai 污染的 json/okhttp/okio jar 已隔离、禁止引入（宪章要求原生 HttpURLConnection）。
- **清单内容（4 类依赖）**：
  1. OpenJFX（JavaFX SDK）21.0.2 (LTS) — GUI/LineChart 运行必选；需 JDK 11+，放 `lib/javafx/`（jar+原生dll，module-path 加载）
  2. mysql-connector-j 8.4.0 (LTS) — `db.type=mysql` 用
  3. sqlite-jdbc 3.47.1.0 — `db.type=sqlite`（宪章首选、零服务）用；推荐常备
  4. JUnit 5.11.4 / Platform 1.11.4（console-standalone 单 jar）— 仅测试
  - 第2/3项二选一驱动；AI 模块无 jar；明确排除 json/okhttp/okio 污染 jar。
- **存放规范**：`lib/javafx/`(JavaFX) + 根目录平铺 JDBC 驱动 + 测试 jar；版本号写入文件名；给出 JDK 兼容矩阵（推荐 JDK21+JavaFX21，仅 JDK8 时改 JavaFX8+mysql 8.0.33）；`.gitignore` 两种策略（A 入库/B 忽略 lib/*.jar）；附编译/运行/测试命令与提交前核对清单。
- **下载直链验证**：3 个 Maven Central 直链均返回 200；Gluon JavaFX 21.0.2 windows-x64 SDK zip 返回 206（资源存在可下载）→ 全部 URL 有效。
- 文件位置：`D:\Users\Desktop\BMI\BMI\ui_lib_record.md`（新增，未跟踪）。

## 输出推送 origin/main 的 Git Bash 执行序列
- **用户指令**：输出完整 Git Bash 序列 —— 拉取远端 main、暂存 db/ src/ docs/ 全部生成资产、提交快照标记备注、推送 origin/main。
- **当前 git 状态（已核实）**：当前分支 `feature-ai`（非 main）；工作区含被隔离删除的 feature-ai 污染文件（AiHealthClient/AiCacheUtil/TestAiService/重复 BodyRecord/lib jar/docs/ai_config.md 均 ` D`）与已删除的泄露密钥 `ai-key.properties`；`main` 本地=origin/main=`eba4b2b`，`origin` 可达（`ls-remote` 成功）。`.gitignore` 行 13-14 已忽略 `db-config.properties`/`ai-key.properties`。
- **关键风险**：AiRequest.java/AiHealthResult.java 仍是 feature-ai 污染版（不编译），main 现有为空桩；两文件快照字节永久丢失。feature-ai 历史含泄露密钥 cfe1efb（勿合并该分支）。
- **交付**：编写 `.workbuddy/git-push-snapshot.sh` 防御型 runbook —— 仓库外备份→`git switch -f main`→`pull --ff-only`→从备份写回→密钥闸门→`git add db/ src/ docs/ CODEBUDDY.md ui_lib_record.md`→人工复核暂停→提交(快照标记)→`push origin main`→打标签 SNAP-20260714-001。强调提交前应先按 ai_design.md §3 重建两 DTO 使 main 可编译。脚本未执行（仅输出）。

## 用户要求"直接可执行"的精简推送脚本（覆写 git-push-snapshot.sh）
- **用户指令**：按指定 4 命令顺序输出可直接在 Git Bash 执行的脚本：pull main → add db/ src/ docs/ CODEBUDDY.md → commit "SNAP-20260714-001 完整归档BMI项目全量源码与规范文档" → push origin main，附 tag。
- **关键修正**：当前在 feature-ai 分支，原样 4 命令会因 `git push origin main` 推本地 main 引用而丢失快照内容；脚本前置 `git switch -f main`（强清 feature-ai 污染、不合并）+ 仓库外备份还原快照资产，保留密钥自动闸门（非交互中止）。去掉了上一版的交互确认，满足"直接执行"。用户 add 清单未含 ui_lib_record.md（按用户指定，未加）。

## feature-ai AI 模块全量静态审查 v2（快照还原后状态）
- **用户指令**：读取当前 feature-ai 分支全部 AI 源码，对照 ai_design.md + plan.md 全量审查，输出 P0/P1/P2 结构化整改清单。
- **关键结论（与还原前审查不同）**：快照还原后 `ai_design.md` 冲突已清除（干净 v1.0），`AiHealthClient/AiCacheUtil/TestAiService` 已隔离（架构分裂 P0-3 已解决）。**根因锁定**：`AiService` 本身符合设计（用丰富 DTO UserMetrics/HistoryTrend/ModelParams/MetricPoint 与 ok()/hasThreeSections()/setFinishReason()），但磁盘上 `AiRequest.java`/`AiHealthResult.java` 是简化桩版（缺上述结构与工厂），导致 **AiService 无法编译**。修复方向=重建这两个 DTO（非改 AiService）。
- **缺陷分级**：P0-1 AiRequest 不符 §3.1-3.4（缺内嵌子结构+setter）；P0-2 AiHealthResult 不符 §4.1（缺三段/usage/finishReason/ok 工厂）；P1-1 历史趋势取最旧N条(`AiService:62-63`，违背"近N次")；P1-2 usage 未解析；P1-3 hasThreeSections 须严格 AC-07 三段齐全；P1-4 缺独立 BmiCalculator/BodyFatCalculator(classify 内联 :295)；P2-1 JSON 转义不全；P2-2 文档残留 AiHealthClient 旧命名；P2-3 AiController 未显式 catch AiConfigException；P2-4 无日志；P2-5 源码全未提交；仓库级🔴 feature-ai 历史含泄露密钥 cfe1efb（须轮换+清理历史+只合到 main）。
- **符合项**：四类异常+重试、双10s超时、Bearer配置外置、五条降级文案逐字一致、错误码常量一致、AiException 继承、AiController 编排、OpenAI 报文格式、上一轮 P0-2/P0-3 已解决。
- 推荐顺序：先重建两 DTO(P0)→P1 功能修复→P1-4 抽取计算类→P2 打磨→仓库级密钥清理+提交 main。报告：`.workbuddy/feature-ai-review-20260714-v2.md`。

## feature-ai AI 模块全量静态审查 v3（远端同步版）
- **用户指令**：读取当前 feature-ai 分支**远端同步后**的 AI 源码，对照 ai_design.md + plan.md 全量审查，输出整改清单。
- **审查对象变更**：当前 detached HEAD 停在 `origin/feature-ai`（HEAD=`2fad333`），比本地 `feature-ai`(`cfe1efb`) 领先 3 个修复提交（a689194 统一AiHealthClient+补齐DTO / a63e028 全面修复P0/P1/P2 / 2fad333 修AiConfigException+BodyRecord）。**审查直接从 git 树 `git show origin/feature-ai:<path>` 提取 8 文件，排除工作区未跟踪污染**。
- **重大改善（相较 v2）**：上一轮 P0-1/P0-2（DTO 缺结构→编译失败）已解决；P0-3 架构分裂已统一为 `AiHealthClient`；P1-1 取最新10条、P1-2 补全指标字段、P1-3 改抛 AiConfigException、P1-5 usage 解析、P1-4 独立 BmiCalculator/BodyFatCalculator —— 全部已修。
- **新增 P0 阻断**：
  - **P0-1 密钥泄露（安全事故）**：`origin/feature-ai` 已提交树含 `ai-key.properties`（commit `cfe1efb` 新增）→ 须**立即轮换 Key + filter-repo/bfg 清理历史 + 禁止合并 main**。
  - **P0-2 白名单违规依赖**：已提交 `lib/json-20240303.jar`(`org.json`被 `AiHealthClient.parseResponse` 引用) + `lib/okhttp-4.12.0.jar`+`lib/okio-3.6.0.jar`（**未使用**）→ 违反 ai_design.md §1「不引入 JSON 库/第三方 HTTP 库」+ plan.md §6.2 无 OkHttp。须移除三 jar 并改纯手工 JSON 解析。
- **P1 须修**：P1-1 `AiException` 基类缺失(`AiConfigException extends Exception` 非 `AiException`)；P1-2 三段分隔符不匹配(systemPrompt 用『』但 extractSection 期望【】→hasThreeSections 恒 false→FR-07 恒降级)；P1-3 `model.ai.BodyRecord` 重复实体+`buildRequest` 签名偏离设计(User,Record,List)；P1-4 `buildJsonBody` 未序列化 historyTrend(FR-07 输入不完整)；P1-5 `AiController` 不在已提交树(controller/ 空)。
- **P2**：TestAiService 非 JUnit；构造函数抛 AiConfigException；plan.md 写 Swing 与宪章 JavaFX 冲突；无日志；分支卫生。
- **编译结论**：`origin/feature-ai` 当前**可编译**（依赖已提交 json jar），但建立在违规依赖上；完成 P0-2+P1-6 手工解析后须同步改 parseResponse 保持可编译。
- 报告：`.workbuddy/feature-ai-review-20260714-v3.md`。

## feature-ai AI 模块全量静态审查 v4（a67f8f9 最新提交）
- **用户指令**：读取 `origin/feature-ai` 最新提交 `a67f8f9` 全部 AI 源码，对照 ai_design.md+plan.md+CODEBUDDY.md 全量审查，输出分级整改（P0 安全/依赖、P1 功能、规范、缺陷清单+阻断合并标记）。
- **审查对象**：`a67f8f9`（origin/feature-ai HEAD）。8 文件：`com.bmi.client.AiHealthClient` / `com.bmi.controller.AiController` / `com.bmi.exception.{AiException,AiConfigException}` / `com.bmi.model.ai.{BmiCalculator,BodyFatCalculator,BodyRecord,TestAiService}`，均从 `git show a67f8f9:<path>` 提取（非工作区）。
- **⚠️ 重大前提**：`a67f8f9` 已是 `origin/main` 祖先 → 代码已合并上线，密钥已落 main。此前"推送 main 安全"判断已不成立。
- **P0 安全（高危，2 项，已落 main）**：
  - P0-S1 `ai-key.properties` 提交含真实明文 Key `sk-9e243736c6294347bf46dcf7d17d2ec6`（gitignore 第19行已列但被强制入库；全历史多提交含此）。
  - P0-S2 `TestAiService.java:19` 硬编码第二把明文 Key `sk-ed48946785d249e3b8349812416ef30e`。
  - 二者均同时存在于 origin/main 与 origin/feature-ai 历史 → 须立即轮换+filter-repo/bfg 清理两分支全历史+移除该文件。
- **P0 依赖 ✅ 通过**：a67f8f9 的 `lib/` 仅 `.keep`（早期 json/okhttp/okio jar 已移除）；AiHealthClient 仅 import JDK，原生 HttpURLConnection+手工 JSON。
- **P1 功能**：P1-F1 分隔符【】统一 ✅；P1-F2 统一 AiException 顶层异常 ✅；P1-F6 请求缺 model/messages/userMetrics（API 必 400 失败）❌；P1-F5 AiController 无 DAO 注入 ❌；P1-F7 四类异常/错误码/重试缺失+结构不符 ❌；P1-F4 历史未截近10条 ❌；P1-F8 降级文案偏差 🟡。
- **规范**：包分层符合用户给定口径(com.bmi.client/exception/model.ai)，但偏离 ai_design §7.1(应 com.bmi.model.ai)；类命名合规；**编译实测 javac17 -encoding UTF-8 0 错误**（重复 import java.net.URI 合法；须 -encoding UTF-8 否则 Windows GBK 报编码错）。
- **BodyRecord 判定修正**：a67f8f9 仅一份 `com.bmi.model.ai.BodyRecord`（迷你重造实体），origin/main 同路径同内容（4 文件 MD5 一致）→ 合并不新增副本，非"两份重复"；降为 P2 治理（包位置偏差+字段不全，全仓无 com.bmi.model.Record/User 实体）。
- 报告：`.workbuddy/feature-ai-review-20260714-v4.md`。

## feature-ai 代码重构与修复 v5（全量重构 a67f8f9，编译 0 错误）
- **用户指令**：全面重构 a67f8f9 的 AI 代码，修复 4 项 P1 功能缺陷(F4/F5/F6/F7)+ReportController:113 类型不匹配+包结构对齐 §7.1+移除硬编码 Key，编译 0 错误并运行时验证。
- **编译结果**：`javac 17 -encoding UTF-8 -d out --module-path lib/javafx --add-modules javafx.controls,javafx.fxml @<23个应用源码>` → **EXIT 0、26 class、0 错误**。
- **实施要点**：
  - 删除孤立 `AiService.java`（引用不存在的 AiRequest/AiHealthResult/AiConfigException，非 a67f8f9 范围）与 `com.bmi.model.ai.BodyRecord` 3字段桩；`com.bmi.client`、`com.bmi.exception` 包清空删除。
  - `AiHealthClient` 迁入 `com.bmi.model.ai`，重写 `buildJsonBody`（P1-F6：补 `model`/`messages`/`userMetrics`，保留 `historyTrend`）；`AiHealthResult` 全字段实体+错误码常量+`ok()`/`fail()`（P1-F7）；`send` 分层降级（断网/超时/参数为空/5xx/429限流/400）；密钥仅读 `ai-key.properties`（零硬编码）。
  - `AiController` 注入 `RecordDao`，新增 `getAdvice(long userId)` 重载内部取数（P1-F5）；`RecordDao.queryLatestN`（P1-F4，DAO 直取最近 10 条翻转为升序）。
  - `ReportController:113` 的 `getAdvice(userId)` 与 `long` 重载天然匹配 → 类型不匹配消除。
  - 新建 `com.bmi.BmiApplication`（JavaFX 入口，烟测用）。
- **运行时验证**：headless 沙箱无显示器 → JavaFX `QuantumRenderer` 初始化失败（环境限制，非代码缺陷）；以无显示冒烟测试驱动 `AiController.getAdvice(long)` 验证 DAO 注入/取数/两条降级路径（数据缺失、密钥缺失→"AI 服务未配置"）均通过。
- **与用户原命令的偏差（已记录，不影响 0 错误）**：①去掉 `-cp "lib/*;lib/javafx/*"` 通配符（Windows 下 `lib/*` 误匹配 `lib/javafx` 子目录致 javac 参数解析报错；JavaFX 须走 module-path）；②排除 `JdbcRecordDaoChainTest.java`（import JUnit5，lib/ 暂无 junit jar，属测试专用依赖）。
- **安全风险再提示**：a67f8f9→main 历史中仍含两把明文 Key（`sk-9e243...`/`sk-ed4894...`）+ `cfe1efb` 提交 `ai-key.properties`；本次重构已实现源码零硬编码，但**历史泄露须另行轮换+filter-repo 清理**，不在本次代码任务范围。
- 报告：`.workbuddy/feature-ai-fix-v5.md`。
