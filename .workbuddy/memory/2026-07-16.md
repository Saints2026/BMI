# 2026-07-16 工作日志

## feature-db 分支 Review 复检

对 `feature-db` 分支（HEAD `6915462`，新提交在原 `432fcf8` 之上）对照 `feature-db-review-opinion.md` 全量复检。

### 已修复项
- DAO 接口方法签名全部对齐（insert/findByUsername/existsUsername public/queryByUser/queryByUserPage(page,size)/deleteById/update/findById）
- predictBodyFat 已在 CalcUtil 中实现
- JUnit4 jar 已删除，替换为 JUnit5 standalone
- db-config.properties.example / ui_lib_record.md / docs/ai_config.md 已恢复
- System.out 打印已清除
- classifyBmi 改为返回枚举（非中文）
- 分页 page/size 边界校验已加
- 新增 db/mysql_init.sql / db/sqlite_init.sql

### 仍存在的关键问题
1. **S1 未修复**：feature-db 分支仍删除全部 UI/AI/controller/i18n/exception/model/ai 文件（25个），diff 仍含大量 deletion
2. **S2 编译错误**：文件名仍为 `UserDAO.java`/`RecordDAO.java`，但接口声明已改为 `UserDao`/`RecordDao` → Java 公共类型名必须与文件名一致，编译会报错
3. **BmiCategory 枚举缺失**：CalcUtil.classifyBmi 返回 BmiCategory 但该枚举文件不存在于 feature-db 任何位置 → 编译错误
4. **R4 超时常量未生效**：CONNECT_TIMEOUT_MS/READ_TIMEOUT_MS 已定义但未调用 setLoginTimeout/setQueryTimeout 应用到连接
5. **R9 文档全量重写**：spec.md 384行/plan.md 596行/db_design.md 944行变更，非增量补充
6. **R2**：CalcUtil 在 model 包而非 model.ai 层
7. diff `origin/main...feature-db` 仍含 46 文件变更（+3702/−4054），非纯 DB 增量

### 结论
不满足合并条件。需在 feature-db 分支上：rebase 到最新 main → 还原被删 UI 文件 → 重命名 DAO 文件 → 补 BmiCategory.java → 应用超时常量。

## Review 问题逐项修复（本地工作区 main 分支）

以下修复在 `main` 分支工作区完成，待后续同步到 feature-db：

### 1. BmiCategory 枚举补建
- 创建 `src/com/bmi/model/BmiCategory.java`（UNDERWEIGHT/NORMAL/OVERWEIGHT/OBESE）
- CalcUtil.classifyBmi 返回类型已匹配，同包无需 import

### 2. CalcUtil 迁移到 model.ai
- 从 `src/com/bmi/model/CalcUtil.java` → `src/com/bmi/model/ai/CalcUtil.java`
- 包声明改为 `package com.bmi.model.ai;`
- 添加 `import com.bmi.model.BmiCategory;`（跨包引用）
- 全局搜索无其他文件引用 `com.bmi.model.CalcUtil`，无需更新外部 import

### 3. JdbcUtil 超时常量应用到连接
- `DriverManager.setLoginTimeout(CONNECT_TIMEOUT_MS / 1000)` → 5 秒登录超时（MySQL+SQLite）
- `conn.setNetworkTimeout(executor, READ_TIMEOUT_MS)` → 10 秒网络超时（仅 MySQL，SQLite 跳过）
- Executor 使用静态 daemon 线程复用

### 4. 文档从 origin/main 恢复并增量更新
- `git checkout origin/main -- docs/spec.md docs/plan.md docs/db_design.md`
- db_design.md 增量更新：
  - 全部 `BmiCalculator`/`BodyFatCalculator` 引用 → `CalcUtil`（7处）
  - 全部 `DbUtil` 引用 → `JdbcUtil`（5处）
  - 新增 §3 BMI 分级枚举描述（BmiCategory 枚举 + i18n 职责边界）
  - 新增 §4.4 连接超时配置（CONNECT_TIMEOUT_MS / READ_TIMEOUT_MS 应用方式表）
  - 附录检查清单新增 2 条（超时常量验证 + CalcUtil/BmiCategory 包路径验证）
- plan.md 同步更新：§4.4 类表替换为 CalcUtil + BmiCategory、架构图/选型/FR映射表同步

### 5. 文档旧类名引用彻底清理（plan.md 补漏 + tasks.md DB 模块）
- plan.md 补漏 3 处 `DbUtil` → `JdbcUtil`（架构图 §4.1、方法表 §4.3 close）
- plan.md 架构图 `Bmi/BodyFatCalc` 缩写 → `CalcUtil`
- tasks.md DB 模块 5 处引用更新：
  - 模块表 L11：DbUtil→JdbcUtil、BmiCalculator/BodyFatCalculator→CalcUtil
  - 分工表 L21：同上
  - 07-14 日程 L33：DbUtil→JdbcUtil
  - 07-16 日程 L35：BmiCalculator/BodyFatCalculator→CalcUtil
  - 07-22 测试 L41：同上
- 最终验证：db_design.md / plan.md / tasks.md 中无任何 BmiCalculator/BodyFatCalculator/DbUtil/Bmi/BodyFatCalc 残留
- 未触碰 ui_design.md / ai_design.md（按要求保留）

### 6. PageResult 分页 DTO 补建 + 接口/实现/测试适配
- 创建 `src/com/bmi/model/db/PageResult.java` 泛型类：data/total/currentPage/pageSize/totalPages（构造方法计算）、仅 getter
- `RecordDao` 接口：两个 `queryByUserPage` 返回类型 `List<BodyRecord>` → `PageResult<BodyRecord>`
- `JdbcRecordDao` 实现：
  - 新增 `countByUser(userId, start, end)` 私有方法（SELECT COUNT(*) + WHERE 条件）
  - 5 参数 `queryByUserPage`：先查总数，再分页查询，构造 `new PageResult<>(list, total, page, size)` 返回
  - 3 参数版本委托 5 参数版本
- `MainTest.java`：test7QueryByPage 适配 `PageResult<BodyRecord>`，添加 import
- 确认：无 controller 层调用 `queryByUserPage`，无其他实现类需同步

### 7. DB 模块全面规范性检查（11 维度）
发现的问题：
- **P0**：`MainTest.java` L38-39 `new UserDao()` / `new RecordDao()` — 接口不能实例化，应改为 `new JdbcUserDao()` / `new JdbcRecordDao()`
- **P1**：`deleteById(long id)` — SQL `WHERE id=?` 缺 `user_id` 条件，违反 spec.md「禁止越权删除」+ plan.md 设计签名 `int deleteById(long userId, long recordId)`
- **P2**：`db-config.properties.example` 缺 `db.driver` 字段（JdbcUtil 需要）；`db.type` 为死配置（JdbcUtil 不读取）
- **P3**：`lib/.keep` 空占位文件；`docs/mysql_schema.sql` 与 `db/mysql_init.sql` 近乎重复（origin/main 已有）；DAO 用 try-finally 而非 try-with-resources

### 8. P0-P3 修复执行
- **P0**：`MainTest.java` 添加 `import JdbcUserDao` / `import JdbcRecordDao`，`new UserDao()` → `new JdbcUserDao()`，`new RecordDao()` → `new JdbcRecordDao()`
- **P1**：`deleteById` 防越权修复
  - `RecordDao.java` 接口：`boolean deleteById(long id)` → `boolean deleteById(long id, long userId)`
  - `JdbcRecordDao.java` 实现：SQL 改为 `DELETE FROM body_record WHERE id = ? AND user_id = ?`，参数 `ps.setLong(2, userId)`
  - `MainTest.java` 调用处：`deleteById(r3.getId())` → `deleteById(r3.getId(), uid)`
  - 确认 `InMemoryUserDao` 实现 `UserDao`（非 RecordDao），无 deleteById 需同步
  - 全局仅 3 处引用（接口+实现+测试），全部已更新
- **P2**：`db-config.properties.example` 重写：添加 `db.driver=com.mysql.cj.jdbc.Driver`，SQLite 注释 `# db.driver=org.sqlite.JDBC`，删除死配置 `db.type=mysql`
- **P3**：删除 `lib/.keep`，lib/ 现仅含 2 个白名单 jar

### 9. 复检中发现并修复的遗留问题
- Windows 大小写不敏感陷阱：`rm UserDAO.java` / `rm RecordDAO.java` 同时删除了 `UserDao.java` / `RecordDao.java`（NTFS 不区分大小写）
- 通过 `git checkout --` 从 index 恢复两个接口文件，但 index 内容为修改前版本
- 重新对 `RecordDao.java` 应用 PageResult 返回类型 + deleteById(long, long) 签名修改
- 额外删除 `docs/.keep` 占位文件
- 编译验证通过：DB 模块 8 文件 + MainTest.java + CalcUtil.java 全部编译成功（`javac -encoding UTF-8 -cp "lib/*"`）
